{
  "name": "bomblok",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BomBlok - block puzzle oyunu",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "check": "npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^10.8.0"
  }
}
